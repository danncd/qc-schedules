# Frontend

The Next.js app behind https://qcs.danncd.com.

```bash
npm install
npm run dev
```

Needs a `.env.local` with `NEXT_PUBLIC_SUPABASE_URL` and a Supabase public key. See `.env.example`.

Setup, architecture and the data pipeline are documented in the [root README](../README.md).
